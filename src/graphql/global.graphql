enum CacheControlScope {
    PUBLIC
    PRIVATE
}

"Gender Enum"
enum Gender {
    MALE
    FEMALE
    OTHER
}

enum UserType {
    USER
    ADMIN
}

"[MailGun Response](https://www.npmjs.com/package/mailgun.js)"
type MailGunResponse {
    id: String
    message: String
    status: Int
    details: String
}
